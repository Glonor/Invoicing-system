<nav id="sidebar">

  <ul class="list-unstyled components">
    <p><%= current_user %></p>
    <li class="<%= current_class?(user_path(current_user)) %><%= current_class?(edit_user_path(current_user)) %>">
      <a class="waves-start" href="#homeSubmenu" data-toggle="collapse" aria-expanded="false">Account</a>
      <ul class="collapse list-unstyled" id="homeSubmenu">
        <li class="waves-start "><%= link_to "Profile", current_user %></li>
        <li class="waves-start "><%= link_to "Settings", edit_user_path(current_user) %></li>
      </ul>
    </li>
    <% if current_user.admin? %>
      <li class="waves-start <%= current_class?(users_path) %>">
        <%= link_to "Users", users_path %>
      </li>
    <% end %>
    <li class="waves-start <%= current_class?(clients_path) %>">
      <%= link_to "Clients", clients_path %>
    </li>
    <li class="waves-start <%= current_class?(invoices_path) %>">
      <%= link_to "Invoices", invoices_path %>
    </li>
  </ul>
</nav>